    name: Error 861 
 
    on:
      workflow_dispatch:
    
    jobs:
      build:
        runs-on: rockylinux/rockylinux:latest
        container:
          image: aswf/ci-base:2023
        steps:
        - name: Set up python 3.11
          uses: actions/setup-python@v5
          with:
            python-version: '3.11'
            cache: 'pip'
