name: Java Tool Version File Testing
on:
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest
    name: Java sample
    steps:
      - uses: actions/checkout@v4
      - name: Setup java
        uses: mahabaleshwars/setup-java@patch-java-version-file
        with:
          distribution: microsoft
          java-version-file: .java-versions
