name: Java Tool Version File Testing
on:
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest
    name: Java sample
    steps:
      - uses: actions/checkout@v4
      - name: Setup java
        uses: mahabalehwars/setup-java@add-tool-version-file-support
        with:
          distribution: 'microsoft'
          java-version-file: '.java-version'
       
